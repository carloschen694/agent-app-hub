# 複製這個檔案為 .env，並填入你自己的值。
# 兩個變數都可選填 —— 沒設定時，改在瀏覽器介面裡輸入即可，不影響其他 App 運作。

# Gemini API Key 的預設值。不設定的話，可在畫面右下角 AI 助理的設定面板貼上，
# 一樣存放在瀏覽器 localStorage，不會經過任何伺服器。
# 取得方式：https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=

# class10-live-assistance 串接 Google 聯絡人／行事曆／Gmail 需要的 OAuth Client ID。
# 只有這個 App 用到；不需要、也不應該設定 Client Secret —— 授權流程完全在瀏覽器端
# 用 Google Identity Services 的 Token Client（implicit flow）完成，Secret 用不到，
# 放進前端專案反而會外洩，Google Cloud Console 建立「網頁應用程式」類型的憑證即可。
# 建立處：https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=
